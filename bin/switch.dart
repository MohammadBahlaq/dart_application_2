void main(List<String> args) {
  ///No need to write [break] keyword after every case
  ///and you can write many statments in one case without {}

  int x = 1;
  switch (x) {
    case 1:
      print(1);
      print("object");
    case 2:
      print(2);
    case 8:
      print(8);
    default:
  }
}
